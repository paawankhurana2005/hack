# Generated / downloaded artifacts (all regenerable) — but keep the tiny trained head
# so the grader works right after a clone (the frozen CLIP backbone downloads from HF).
data/
!data/model_best.pt

# Local server / run logs
*.log

# Python
__pycache__/
*.pyc
.venv/
venv/
*.egg-info/
