# Dynamic-pricing ML stack (spec 014). Tabular → trees, no deep learning.
numpy>=1.26
pandas>=2.0
scikit-learn>=1.3
xgboost>=2.0   # Phase 2 (warm-start reward model)
shap>=0.44     # Phase 2 (feature importances — the defensible "why")
