# caches, downloaded sample data, checkpoints, reports
__pycache__/
*.pyc
.cache/
runs/
*.pt
eval_report.json

# local full dataset copies (only the grading-root data/ dir, NOT nested data/ dirs
# like the committed datasets/kaputt_mini/data crops)
/data/
