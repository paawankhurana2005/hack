{
  "name": "reloop",
  "version": "0.0.0",
  "private": true,
  "description": "ReLoop — an AI layer that gives returned/unused products a second life.",
  "scripts": {
    "dev:web": "pnpm --filter @reloop/web dev",
    "dev:api": "pnpm --filter @reloop/api dev",
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "eval": "pnpm --filter @reloop/api eval",
    "test:edge": "pnpm --filter @reloop/api test:edge"
  },
  "packageManager": "pnpm@9.12.0",
  "engines": {
    "node": ">=18.18.0"
  },
  "dependencies": {
    "playwright": "^1.60.0"
  }
}
